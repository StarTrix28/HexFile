from setuptools import setup

setup(
    name='hexfile',
    version='1.0',
    packages=['ast'],
    license='OpenSource',
    author='Trix28',
    author_email='trix28.code@gmail.com',
    description='store information in files encrypted using hexadecimal code'
)
